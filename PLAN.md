# 인터랙티브 프론트엔드 직무 발표 계획

## 목적

대학생 청중에게 프론트엔드 개발자의 실제 업무와 AI 활용을 소개하는 약 10분 발표를 만든다. 발표 자료 자체가 화면을 동작하게 만드는 프론트엔드의 특징을 보여주도록, PowerPoint 대신 Vite·TypeScript·reveal.js 기반의 오프라인 웹 프레젠테이션으로 제작한다.

## 현재 구현된 범위

- 배터리 충전형 준비 화면: 녹색 충전, 완료 후 검정색 전환, 시작 버튼 없음
- 진로 선택 도입 장면: 학사모 클릭 후 학점 3.26과 게임패드 물풍선, 컴퓨터와 플라스크 아이콘을 순서대로 표시
- 캐릭터를 드래그해 플라스크와 겹치면 붉은 배경, 컴퓨터와 겹치면 연두색 배경을 표시하고 컴퓨터 위에서 놓으면 근무 연혁 지도로 전환
- 근무 연혁 지도: 서울 25개 구와 성남시 분당구만 표시하는 로컬 SVG
- 근무 이력 단계:
  - 2016.11–2019.07: 판교 Exntu
  - 2019.08–2020.05: 강남구 KINX
  - 2020.05–현재: 분당 중심 프리랜서
- 핀은 지도에 고정된 끝점에서 솟아오르고, 분당구 영역은 한 덩어리로 튀어나오며 강조된다.
- 현재 단계의 회사명과 기간만 지도 오른쪽에 표시한다.
- 이전 단계 이동, 진로 장면까지의 초기화, 길게 누른 키 무시, 모션 감소 설정을 지원한다.
- PDF에서는 로딩·진로 선택·조작 UI를 제외하고 전체 이력을 한 페이지에 표시한다.

## 발표 흐름

1. 배터리 준비 화면
2. 학사모를 눌러 학점·취미를 확인하고 진로를 선택하는 장면
3. 근무 연혁 지도
4. 프론트엔드란?
5. 카페 키오스크 역할 시연
6. 반도체 분야의 프론트엔드 업무
7. AI를 활용하는 두 가지 방법
8. 개발 방식 Before / After
9. AI 시대에도 남는 문제 해결 능력
10. AI에게 맥락을 제공하는 방법
11. AI를 직접 사용해 본 결론

현재 저장소에는 1–3번만 구현되어 있다. Q&A 장면은 포함하지 않는다.

## 다음 구현 순서

1. `프론트엔드란?`: Design → Frontend → Backend 세 역할을 짧은 문장과 도식으로 표시
2. `카페 키오스크`: 결제 버튼으로 매출·재고·완성 수량이 변하는 시뮬레이션 구현
3. `반도체 업무`: 가상 Cell Library, Scribe Lane, Layout Canvas, Drag & Drop, Pan, Zoom 구현
4. `AI 활용`: 프로그램이 AI를 사용하는 경우와 개발자가 AI를 사용하는 경우를 분기 화면으로 표현
5. `Before / After`: Fragment로 탐색·구현 과정 변화를 순차 표시
6. `문제 해결 능력`: 문제 파악·판단·검증을 핵심 메시지로 구성
7. `Context`: 여행 조건 Chip을 추가하며 Prompt가 구체화되는 인터랙션 구현
8. `결론`: 더 좋은 도구와 부사수라는 개인 경험을 시각적으로 정리

## 기술·운영 원칙

- 외부 API, 실제 회사 코드, 실제 반도체 설계 데이터는 사용하지 않는다.
- 지도·폰트·SVG·JavaScript 라이브러리는 로컬에서 번들한다.
- 실제 AI 모델, 결제 서버, DB, 반도체 계산 모델은 구현하지 않는다.
- 모든 인터랙션에는 정적인 최종 상태가 있어야 하며 PDF에서도 내용을 이해할 수 있어야 한다.
- `prefers-reduced-motion`, 인쇄 모드, 발표자 노트 미리보기를 계속 지원한다.

## 실행과 검증

```sh
npm ci
npm run dev
npm run build
npm test
npm run pdf
```

완료 기준은 각 장면의 핵심 메시지와 인터랙션이 오프라인에서 동작하고, 10분 발표 흐름이 자연스럽게 이어지며, PDF 한 장에 제출 가능한 상태로 출력되는 것이다.
